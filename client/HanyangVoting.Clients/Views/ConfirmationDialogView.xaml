<Window x:Class="HanyangVoting.Clients.Views.ConfirmationDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        MinHeight="200" Width="630" ResizeMode="NoResize"
        Title="{Binding Title}" ShowInTaskbar="False"
        SizeToContent="Height" WindowStartupLocation="CenterOwner"
        FontFamily="서울남산체 EB"
        >
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <ContentPresenter Content="{Binding Content}" Margin="10,10,10,10" Grid.Row="0" />
        
        <Button Content="아니오" HorizontalAlignment="Left" Padding="6" Width="100" Margin="7,12,0,7" Grid.Row="1" FontSize="18">
        </Button>
        <Button Content="예" HorizontalAlignment="Right" Padding="6" Width="100" Margin="7,12,0,7" Grid.Row="1" FontSize="18">
        </Button>
    </Grid>
</Window>
